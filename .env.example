OPENAI_API_KEY=sk-your-key-here

# Optional: AI workspace (Notion, Slack). When human company is added, use AI_* vs HUMAN_*.
# AI_NOTION_API_KEY=
# AI_NOTION_DATABASE_ID=   # DB must have: Company (Title), Role (Text), Task (Text)
# AI_SLACK_BOT_TOKEN=
# AI_SLACK_CHANNEL=#ai-agents
# AI_SLACK_USE_COMPANY_CHANNELS=true   # Auto-create #ai-{company-slug}, join if exists. Add scopes: channels:manage, channels:read
